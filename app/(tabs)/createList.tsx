import { Text, View, StyleSheet } from "react-native";
import React from "react";

const CreateList = () => {
  return (
    <View>
      <Text>This is the create list page</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});

export default CreateList;
